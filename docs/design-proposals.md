# Design proposals

Create a discussion or issue using the architecture proposal template before changing process boundaries, command contracts, state semantics, persistence format, plugin loading, or platform-support commitments.

A proposal must describe context, goals, non-goals, alternatives, compatibility, operational risks, security impact, test strategy, migration, and ownership. Accepted proposals become ADRs under `docs/adrs/`.
