# Third-party notices

The core runtime uses only the .NET shared framework. The desktop HMI uses Avalonia UI under the MIT License.
The optional Python reference implementation has dependencies listed in `reference/python-simulator/pyproject.toml`.

Release builds should generate a software bill of materials and preserve all dependency notices.
