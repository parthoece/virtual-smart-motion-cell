# Software bill of materials

`source.spdx.json` is a source-tree SPDX 2.3 inventory generated by `scripts/generate_sbom.py`.

Release workflows generate a separate SBOM beside each platform package. The source SBOM is reproducible and intended for review; release attestations bind published artifacts to the GitHub workflow that produced them.
