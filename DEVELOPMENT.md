# Development

See the [contributor quickstart](docs/contributor-quickstart.md) for prerequisites, local startup, test commands, repository conventions, and troubleshooting.

The shortest validation command is:

```bash
./scripts/dev-check.sh
```

Windows PowerShell:

```powershell
pwsh ./scripts/dev-check.ps1
```
